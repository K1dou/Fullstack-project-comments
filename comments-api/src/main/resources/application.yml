spring:
  application:
    name: comments-api
  datasource:
    url: jdbc:postgresql://localhost:3306/comments
    username: root
    password: root
    driver-class-name: com.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
